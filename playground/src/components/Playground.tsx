import { defineComponent } from "vue";

export default defineComponent({
    name: 'PlayGround',
    setup() {
        return () => (
            <div>
                <h1>PlayGround</h1>
            </div>
        )
    }
})